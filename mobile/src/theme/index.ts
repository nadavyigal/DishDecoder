import colors, { opacity } from './colors';
import spacing, { layout } from './spacing';
import typography, { textStyles } from './typography';

export const theme = {
  colors,
  spacing,
  layout,
  typography,
  textStyles,
  opacity,
};

export { colors, spacing, layout, typography, textStyles, opacity };
export default theme; 